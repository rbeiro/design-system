import {
  config,
  createTheme,
  getCssText,
  css,
  globalCss,
  keyframes,
  theme,
  styled,
} from "@rbeiro-ui/stitches-config";

export {
  config,
  createTheme,
  getCssText,
  css,
  globalCss,
  keyframes,
  theme,
  styled,
};
